---
- name: Converge
  hosts: all
  tasks:
    - name: "Include lpars.power.lpar and run configure_netboot_server.yml"
      ansible.builtin.include_role:
        name: lpars.power.lpar
        tasks_from: configure_netboot_server.yml
